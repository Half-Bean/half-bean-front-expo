import React from "react";
import { SafeAreaView, ScrollView, Text } from "react-native";

const NoticeListScreen = () => {
  return (
    <ScrollView>
      <SafeAreaView>
        <Text>NoticeListScreen</Text>
      </SafeAreaView>
    </ScrollView>
  );
};

export default NoticeListScreen;
