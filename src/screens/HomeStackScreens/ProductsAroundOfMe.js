import React from "react";
import { SafeAreaView, ScrollView, Text } from "react-native";

const ProductsAroundOfMe = () => {
  return (
    <ScrollView>
      <SafeAreaView>
        <Text>ProductsAroundOfMe</Text>
      </SafeAreaView>
    </ScrollView>
  );
};

export default ProductsAroundOfMe;
